# typescript-5mins-demo

0 setup tsc 

  ```code
    npm i typescript -g
  ```
1 install

  ```code
    npm i
  ```

2 run live compile

  ```code
    npm start
  ```

3 run compile

  ```code
    npm run compile
  ```